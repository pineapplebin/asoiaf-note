import Layout from '../layout.html.jsx'
import chapMeta from './meta.json'
import { getSiteRoot } from '../../utils/tools.js'

export const meta = {
  vol: 1,
  id: 'chapters-vol-1',
  title: '权力的游戏',
}

export const layout = props => {
  const root = getSiteRoot(props.environment)
  return (
    <Layout meta={meta} hasCover={false} {...props}>
      <h1>权力的游戏章节</h1>
      <ol start="0">
        {chapMeta.map(chap => (
          <li key={chap.index}>
            <a href={root + chap.link}>{chap.title}</a>
          </li>
        ))}
      </ol>
    </Layout>
  )
}
